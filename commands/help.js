const Discord = require('discord.js')

module.exports.run = async (client, message, args) => {
  if (!args) {
    return
  } else {
    return
  }
}

//The command's name
module.exports.help = {
  name: "help"
}